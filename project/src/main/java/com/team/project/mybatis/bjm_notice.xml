<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
    <!-- Post-Register -->
    <insert id="post" parameterType="NoticeVo">
          insert into notice(id, title, content) values(#{id}, #{title}, #{content})
    </insert>
    <!-- Post-AttRegister -->
    <insert id="postAtt" parameterType="NoticeAtt">
        insert into noticeAtt(noticeId, oriFile, sysFile) values
        <!-- attFiles로 저장 -->
        <foreach collection="attFiles" item="f" separator=",">
            (#{noticeId}, #{f.oriFile}, #{f.sysFile})
        </foreach>
    </insert>
    <!-- Post-Modify -->
    <update id="modify" parameterType="NoticeVo">
        update notice set title=#{title}, content=#{content}
        <!-- sno로 할지 id로 할지? 어차피 selectOne으로 찾아서 넣는거니 생각좀해보자 -->
        where sno=#{sno}
    </update>
    <!-- Post-hit -->
    <update id="postHit" parameterType="Integer">
        update notice set hit=hit + 1 
        where sno=#{_parameter}
    </update>
    <!-- search -->
    <select id="search" parameterType="NoticePage" resultType="NoticeVo">
        select sno, id, title, hit, contetnt, date_format(nal, "%Y-%m-%d") nal,
        concat(lapd(' ', deep*12, <![CDATA['&nbsp;&nbsp;']]>), title) title from notice
        <where>
            <if test="findStr != null and findStr != ''">
                <!-- concat을 사용한 sql인젝션방지? 검색에서 필요할까? -->
                (title like CONCAT('%', #{findStr}, '%')
                or content like CONCAT('%', #{findStr}, '%'))
            </if>
        </where>
        order by sno desc
        limit #{startNo}, #{listSize}
    </select>
    <!-- pagingSize -->
    <select id="totSize" parameterType="String" resultType="Integer">
        select count(sno) from notice
        <where>
            <if test="findStr != null and findStr != ''">
                id like '%${_parameter}%'
                or title like '%${_parameter}%'
            </if>
        </where>
    </select>
    <!-- delete -->
    <select id="delete" parameterType="String" resultType="NoticeVo">
        delete from notice where sno=#{_parameter}
    </select>
    <!-- view -->
    <select id="view" parameterType="String" resultType="">
        select sno, title, content, id, hit, nal from notice
        where sno=#{_parameter}
    </select>
    
</mapper>
