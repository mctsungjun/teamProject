<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="msmQa">
    <select id="list" parameterType="String" resultType="com.team.project.msm.msmQaVo">
        select qusId, qusDate, qusTitle, qusNum, qusView
        from qa
        order by qusNum desc
    </select>

    <select id="view" parameterType="Integer" resultType="com.team.project.msm.msmQaVo">
        select * from qa
        where qusNum = #{qusNum}
    </select>

    <select id="qaFiles" parameterType="Integer" resultType="com.team.project.msm.msmQaVo">
        select qusId, qusCon, qusDate, qusTitle, ansId, ansCon, ansDate
        from qa
    </select>

    <select id="detail" resultType="com.team.project.msm.msmQaVo">
        select qusId, qusCon, qusDate, qusTitle, ansId, ansCon, ansDate
        from qa
    </select>
</mapper>



